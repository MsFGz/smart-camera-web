name: smart_camera_web
description: A smart camera web app
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  camera_web: ^0.3.2
  flutter_web_plugins:
    sdk: flutter
```


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
